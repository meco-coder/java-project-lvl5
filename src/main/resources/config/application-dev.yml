server:
  error:
    include-stacktrace: on_param

spring:
  datasource:
    url: jdbc:h2:./hexlet
    username: sa
    password: sa

  jpa:
    show-sql: true

  h2:
    console:
      enabled: true
      path: /h2console/
      settings:
        web-allow-others: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    root: INFO
    web: DEBUG